<h1>Songs</h1>

<!-- Add code here to list a category's posts. -->

<ul>
  <% @songs.each do |song| %>
    <li><%= link_to "#{song.artist_name} - #{song.title}", song_path(song) %></li>
  <% end %>
</ul>